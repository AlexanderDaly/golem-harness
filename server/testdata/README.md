# Testdata Policy

This directory contains only sanitized or placeholder fixture material.

Raw telemetry, even synthetic raw UI text, is generated in bounded in-memory test scope and must not be stored here. This preserves the Phase 1 boundary that unsanitized telemetry is never written to disk.
